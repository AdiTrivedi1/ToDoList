
$(function () {
    // $('li.hot').addClass('test');

    // $('li:lt(3)').hide().slideDown(1200);

    // $('li[id!="one"]')
    // .hide()
    // .delay(400)
    // .fadeIn(1400);

    // var $listHTML = $('li').text();
    // text() method retrieves info from all the items in the matched set but only the text content. 
    // $('li').append($listHTML);
    // append is updating the content so it updates in all the items.
});